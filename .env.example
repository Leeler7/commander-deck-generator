# Email Configuration (optional — required only for the contact form)
RESEND_API_KEY=your_resend_api_key_here

# Tagger data URL — S3 bucket with Scryfall oracle tag data (role detection)
TAG_REPO_URL=https://mtg-deck-builder-tagger.s3.amazonaws.com/tagger-tags.json
